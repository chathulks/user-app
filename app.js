function OnclickNewUser(){

    let u_id = document.getElementById("u_id").value;
    let u_id = document.getElementById("u_id").value;
    let u_id = document.getElementById("u_id").value;
    let u_id = document.getElementById("u_id").value;

    let user = {
        "userId": u_id,
        "emailId": u_email,
        "fullName": u_fulln,
        "password": u_pw
    }
}